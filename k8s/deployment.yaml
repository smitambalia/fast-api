# Backward-compatible entry: prefer applying all k8s resources.
# New layout:
#   kubectl apply -f k8s/namespace.yaml -f k8s/api.yaml -f k8s/web.yaml
# Or: kubectl apply -k k8s/
#
# This file re-applies API only for older scripts; CI now uses api.yaml + web.yaml.
apiVersion: v1
kind: List
items: []
